export interface loginProps {
  email: string;
  password: string;
}

export interface todoListProps {
  content: string;
  createdAt: string;
  id: string;
  title: string;
  updatedAt: string;
}

export interface simpleTodoProps {
  title: string;
  content: string;
}
